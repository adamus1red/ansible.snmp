---
# vars file for sbaerlocher.snmp

snmp_packages:
  - net-snmp
  - snmp-mibs

snmp_config: '/etc/snmp/snmpd.conf'
snmp_daemon_config: '/etc/default/snmpd'
snmp_deamon_group: 'root'
snmp_deamon_user: 'root'
snmp_logging_options: '-Lsed'
snmp_service_name: 'snmpd'
